# Trace Viewer

复用春季课程的 React + Vite 课件界面。安装、编译及课件编写方法见 [仓库说明](../../README.md)。

```bash
npm ci
npm run dev -- --host 127.0.0.1
```

默认打开模拟课件 `lecture_00`。启动前请在 `lectures/` 运行 `python execute.py -m lecture_00`。
